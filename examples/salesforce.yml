salesforce:
  default_args:
    owner: "custom_owner"
    start_date: 2 days
  description: "Testing Salesforce DAG"
  schedule_interval: "0 3 * * *"
  tasks:
    task_1:
      generator: dagfactory.generators.AirbyteDbtGenerator
      airflow_connection_id: airbyte_conn_example
      dbt_project_path: /app/dbt/
      run_dbt_deps: False
